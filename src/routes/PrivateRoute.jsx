/**
 * PrivateRoute — Step 4 stub.
 * Authentication guard will be wired in Step 5.
 * For now, all routes are accessible so routing can be tested.
 */
export default function PrivateRoute({ children }) {
  // Step 5: replace with auth check + redirect to /login
  return children
}
